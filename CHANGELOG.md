# Changelog

## 2026-04-28

- Broken: the default background ingestion cadence was 2 minutes instead of the required 5 minutes; source inventory ranking was not query-family aware enough for Data Analyst; Lever normalization kept only one location even when public payloads exposed multiple locations; ambiguous state/country codes and generic legal text could misclassify foreign locations as US; seniority could not represent manager, director, or executive; crawler benchmark output omitted required title, seniority, location, and expired-job diagnostics; fast crawls still started supplemental discovery after baseline had already met the target result count.
- Changed: default recurring ingestion now runs every 5 minutes, inventory ranking uses role/coverage/location health signals, Greenhouse registry records carry coverage metadata, Lever jobs preserve all location evidence, US resolution handles ambiguous country/state evidence more conservatively, title retrieval supports modifier-qualified analyst concepts without broadly accepting unrelated roles, seniority supports distinct manager/director/executive levels, benchmark reports per-scenario distributions plus expired-job counts, abort listener fan-out is quiet under high concurrency, and fast crawls skip supplemental discovery once baseline persistence reaches the configured target.
- Metric improved: latest full benchmark completed without hanging in 215.3s wall / 209.7s crawl, saved 118 jobs to MongoDB, returned 122 DB-backed search results, reported title/seniority/location distributions for all 4 required scenarios, and had 2 prevented canonical-URL duplicates, 0 invalid URLs, and 0 expired jobs. Data Analyst recovered from the prior low-recall path to 15 DB-backed results in the full run, with a focused verification run at 14 saved / 14 DB results after tightening foreign-location handling.
- Remaining: live provider reliability still depends on public board latency. Several public boards still hit bounded provider/source/global timeouts, Machine Learning Engineer remains the lowest-recall live scenario, and Workday remains covered by best-effort connector tests/diagnostics rather than the fast benchmark path.
